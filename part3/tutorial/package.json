{
  "name": "tutorial",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "build:full": "rm -rf dist/ && cd ../../part2/tutorial && npm run build && mv dist/ ../../part3/tutorial"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^9.0.0"
  }
}
